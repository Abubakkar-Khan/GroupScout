export interface PostData {
  groupId: string;
  groupName: string;
  postId: string;
  url: string;
  content: string;
  timestamp: string;
  author: string;
}

export interface Keyword {
  keyword: string;
}
